import type React from 'react';

// This component is no longer in use, but is kept to prevent build issues
// that can sometimes arise from deleting files referenced in a build cache.
const GameStats: React.FC = () => {
  return null;
};

export default GameStats;
